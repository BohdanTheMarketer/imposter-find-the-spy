// Secrets.example.xcconfig
// Committed template — copy this file to `Secrets.xcconfig` (same folder) and fill in
// your own values. `Secrets.xcconfig` is gitignored so real keys never reach git history.

OPENAI_API_KEY = your-openai-api-key-here
